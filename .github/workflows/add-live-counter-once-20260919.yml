name: Add live catalog counter

on:
  push:
    paths:
      - '.github/workflows/add-live-counter-once-20260919.yml'

permissions:
  contents: write

jobs:
  add-live-counter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "This workflow has already been replaced by the validated live-counter commit path."
